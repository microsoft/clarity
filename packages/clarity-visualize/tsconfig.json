{
    "extends": "../../tsconfig.json",
    "compilerOptions": {
        "baseUrl": ".",
        "paths": {
            "@src/*": ["src/*"],
            "@clarity-types/*": ["types/*"]
        }
    },
    "include":["src/**/*.ts", "src/**/*.css", "types/**/*.d.ts", "rollup.config.ts"],
    "exclude": ["node_modules", "build"]
}
