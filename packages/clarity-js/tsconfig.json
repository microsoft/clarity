{
    "extends": "../../tsconfig.json",
    "compilerOptions": {
        "baseUrl": ".",
        "paths": {
            "@src/*": ["src/*"],
            "@clarity-types/*": ["types/*"]
        }
    },
    "include":["src/**/*.ts", "types/**/*.d.ts", "rollup.config.ts"],
    "exclude": ["test", "node_modules", "build"]
}
